import React from 'react';
import PropTypes from 'prop-types';

const Header = props => {
    return (
        <div>
            This is the header
        </div>
    );
};

Header.propTypes = {

};

export default Header;